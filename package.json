{
  "name": "certchain-monorepo",
  "version": "1.0.0",
  "description": "CertChain Monorepo Runner to start both frontend and backend together",
  "main": "server.js",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
